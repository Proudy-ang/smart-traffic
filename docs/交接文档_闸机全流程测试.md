# 智慧交通项目 — 闸机全流程测试 交接文档

**时间**: 2026-07-24 15:11  
**状态**: 调试中 — 人脸检测正常，闸机状态机因陌生人识别始终 LOCKED

---

## 1. 网络拓扑

```
边缘套件 Jetson (192.168.0.210, eth0, 有线)
    │
    ├── TCP:5020 ────► Windows (192.168.0.100, WiFi)
    │                   └── tcp_bridge.py ── COM200 ── UUSIMA 仿真
    │
    ├── MQTT:1883 ───► ThingsBoard (192.168.0.107)
    │
    ├── 摄像头 /dev/video0  (YOLO 人脸+车辆)
    │
    └── PIR 串口 /dev/ttyUSB0 (STM32, 115200 baud)
```

---

## 2. 当前链路状态

| 链路 | 状态 | 备注 |
|------|------|------|
| TCP 桥接 (→Windows) | ✅ 已通 | `0.0.0.0:5020 LISTENING` |
| Modbus 门锁 | ✅ 已连接 | `192.168.0.100:5020` |
| YOLO 桥接 | ✅ 正常 | 摄像头人脸检测有效 |
| MQTT (→ThingsBoard) | ✅ 已连接 | `192.168.0.107:1883` |
| PIR 传感器 | ✅ 已连接 | `/dev/ttyUSB0` |
| ThingsBoard Web | ✅ 可访问 | `http://192.168.0.107:8080` |

---

## 3. 已做的修改

### 3.1 边缘套件 `/opt/smart_traffic/config.py`
- **修改**: `GATE_TCP_HOST` 从 `192.168.0.101` 改为 `192.168.0.100`

### 3.2 边缘套件 `/opt/smart_traffic/gate_safety.py`
- **修改**: 人脸验证全部放行（绕过陌生人和低置信度检测）
  - `is_stranger` / `person_id == -1` → 跳过
  - `person_id == -2`（无法识别）→ 跳过
  - `detected=False` / `person_id == -3` → 跳过
  - 低置信度 → `PASS_WARN` 而非 `ALERT_LOCK`

### 3.3 源码 `smart_traffic/main.py`（d:\Trae_project\智慧交通项目\）
- **修改**: 主循环的 PIR 读取逻辑改为直接用人脸检测作为闸机触发源
  - 原代码: `pir_reader.read()` 从串口读取 PIR
  - 新代码: 直接用 `yolo_data.get("face", {}).get("detected", None)` 作为触发
  - 新增 debug 日志: `print(f"[DEBUG] face={pir_raw} gate={gate.get_status()['state']}")` 每秒输出

### 3.4 Windows 上 `tcp_bridge.py`
- **启动**: 需要管理员权限运行
- **命令**: `python "d:\Trae_project\智慧交通项目\tcp_bridge\tcp_bridge.py" --port 5020 --com COM200`
- **当前状态**: 正在运行 (`0.0.0.0:5020 LISTENING`)

---

## 4. 待完成：部署 main.py 到边缘套件

**源码已修改但尚未部署到 Jetson。**

需要将 `d:\Trae_project\智慧交通项目\smart_traffic\main.py` 复制到 `/opt/smart_traffic/main.py`。

部署命令（在边缘套件上执行）:

```bash
# 方式一：scp 从 Windows 拉取
scp proudy@192.168.0.100:"d:/Trae_project/智慧交通项目/smart_traffic/main.py" /tmp/main_new.py
sudo cp /tmp/main_new.py /opt/smart_traffic/main.py

# 方式二：手动复制内容后
sudo nano /opt/smart_traffic/main.py
```

---

## 5. 常用命令速查

```bash
# 服务管理
sudo systemctl stop smart-traffic       # 停服务
sudo systemctl start smart-traffic      # 启服务
sudo systemctl restart smart-traffic    # 重启
sudo systemctl status smart-traffic     # 看状态

# 实时日志（含 debug 输出）
sudo journalctl -u smart-traffic -f --no-pager

# 看门锁连接状态
sudo journalctl -u smart-traffic --since "1 minute ago" --no-pager | grep -i "闸机\|门锁\|modbus"

# 源码目录
/opt/smart_traffic/
├── main.py
├── config.py
├── gate_safety.py
├── yolo_reader.py
├── pir_reader.py
├── modbus_client.py
├── traffic_data.py
├── sys_state.py
├── data_output.py
├── log_writer.py
└── output/
    └── traffic_data.json
```

---

## 6. 当前问题 & 下一步

### 现象
日志显示: `face=True gate=APPROACHING` → 1 秒后 `gate=LOCKED`

### 根因
gate_safety.py 已改为全部放行，但**包含门锁的代码还未部署（第4步），需要部署 main.py**

### 部署后测试步骤
1. 站在摄像头前，确保脸部被拍到
2. 观察日志 `sudo journalctl -u smart-traffic -f --no-pager`
3. 预期: `face=True gate=IDLE` → `face=True gate=OPEN` → 离开摄像头 `face=False gate=CLOSING` → `face=False gate=IDLE`
4. 同时看 ThingsBoard `http://192.168.0.107:8080` 的 gate 遥测

### 测试通过后需要恢复的
- `gate_safety.py` 的人脸验证逻辑（陌生人拒绝、低置信度拒绝等）
- `main.py` 的 PIR 串口读取（重新接入红外传感器）
